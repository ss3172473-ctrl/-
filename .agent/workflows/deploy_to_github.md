---
description: Deploy current changes to GitHub
---

// turbo-all
1. Add all changes to staging
git add .

2. Commit changes (Agent should replace message with relevant summary)
git commit -m "Update project files"

3. Push to remote repository
git push
